# Build artifacts
bin/
dist/
plugin/plugin
*.so
*.dylib
*.dll
*.a
*.o
*.out

# Go
plugin/vendor/
*.test
*.prof
coverage.out
coverage.html
# Optional monorepo sibling overlay (see go.work.example)
go.work
go.work.sum

# Local config / state
config.yaml
*.sqlite
*.sqlite-*
.cortext/
.DS_Store

# Local MDScript goals / agent scratch (not published with the plugin)
.mdscripts/

# Live eval outputs (may embed operator home paths; regenerate via VERDICT.md)
bench/**/out/
bench/**/gaps_out/
bench/judge_eval/out/
bench/e2e_blackbox/out/
bench/e2e_blackbox/gaps_out/


# IDE
.idea/
.vscode/
*.swp
*~

# Cortext native drop-ins (large; document how to fetch)
vendor/darwin-*/
vendor/linux-*/
vendor/windows-*/
vendor/models/
!vendor/README.md

# Python bench caches
__pycache__/
*.pyc
